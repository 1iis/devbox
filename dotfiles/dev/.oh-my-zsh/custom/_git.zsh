# ============================================================================
# Git                                                                    BEGIN
# ============================================================================

# Sync / Save ... Squash!
alias 's...'='git status && git add -A && git commit -m "save" && git pull && git push && git log -n 1 && git status'
alias '...s'='echo "Squashing those \".\" commits!"'


# ============================================================================
# END                                                                      Git
# ============================================================================
